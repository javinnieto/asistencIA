# Mosquitto Configuration for AsistencIA
# Permite conexiones anónimas (sin user/password)
allow_anonymous true

# Listener en el puerto estándar MQTT
listener 1883

# Listener WebSockets (para frontend si fuera necesario)
listener 9001
protocol websockets

# Logging
log_type all
log_dest stdout
